## Java Calculator:

This is a simple calculator application built using Java Swing. It features a user-friendly interface with a display area and buttons for digits, operations, and functions. The calculator supports basic arithmetic operations such as addition, subtraction, multiplication, division, and square root.

### Features:

- Basic arithmetic operations: +, -, \*, /
- Square root function
- All Clear (AC) functions

### Usage:

1. Run the `Calculator` class to launch the application.
2. Use the buttons to input numbers and perform calculations.
3. The result will be displayed in the display area.

### Note:

- The calculator currently does not support advanced functions like memory storage. Future updates may include these features.
